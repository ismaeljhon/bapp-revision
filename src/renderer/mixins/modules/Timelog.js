let Timelog = {
    methods: {
        recordTimelog(timeConsumed) {
            console.log('Record Time', timeConsumed);
        }, 
        pushTimelog() {

        }
    }
}

export default Timelog;